[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "PLD_accounting"
version = "0.5.0"
description = "Numerical privacy accounting for random allocation and subsampling using PLDs."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
  { name = "Moshe Shenfeld" }
]
maintainers = [
  { name = "Moshe Shenfeld" }
]
urls = { Homepage = "https://github.com/moshenfeld/PLD_accounting", Repository = "https://github.com/moshenfeld/PLD_accounting", Issues = "https://github.com/moshenfeld/PLD_accounting/issues" }
keywords = [
  "differential privacy",
  "privacy accounting",
  "privacy loss distribution",
  "pld",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Security",
  "Topic :: Scientific/Engineering :: Mathematics",
]
dependencies = [
  "numpy>=1.23",
  "scipy>=1.10",
  "numba>=0.58",
  "dp-accounting>=0.4.3",
  "random_allocation>=1.0.4",
  "typing-extensions>=4.0",
]

[project.optional-dependencies]
test = [
  "pytest>=8.0",
  "pytest-cov>=4.0",
  "mypy>=1.10",
]
dev = [
  "pytest>=8.0",
  "pytest-cov>=4.0",
  "mypy>=1.10",
  "build>=1.2.0",
  "pylint>=4.0",
  "pylint-per-file-ignores>=3.0",
]

[tool.setuptools.packages.find]
include = ["PLD_accounting*"]
